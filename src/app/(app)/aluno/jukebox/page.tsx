// Ficheiro: src/app/(app)/aluno/jukebox/page.tsx
import JukeboxClientPage from './jukebox-client';

export default function JukeboxPage() {
  // Apenas renderiza o componente de cliente que fará todo o trabalho.
  return <JukeboxClientPage />;
}