// Ficheiro: src/app/(app)/aluno/atividades/page.tsx (VERSÃO FINAL - SIMPLES)
import AtividadesClientPage from './atividades-client';

export default function MinhasAtividadesPage() {
  // Esta página agora apenas renderiza o componente de cliente.
  // Nenhuma busca de dados ou lógica do servidor acontece aqui.
  return <AtividadesClientPage />;
}